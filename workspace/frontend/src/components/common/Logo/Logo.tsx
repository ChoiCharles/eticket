import React from 'react';
import { IconButton } from '@mui/material';

const Logo = () => {
  return (
    <div>
      <IconButton>ETICKET</IconButton>
    </div>
  );
};

export default Logo;
