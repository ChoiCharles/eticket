spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:33066/eticket
    username: root
    password: ssafy
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  # Mapper 간의 순환 참조가 필요 해서 넣어 뒀는데 나중에 괜찮으면 빼겠습니다.
  #main:
   # allow-circular-references: true
eticket:
  redis:
    auth:
      host: localhost
      port: 36379
      database: 1
  auth:
    jwt:
      insecure: true
      insecure-key-path: insecure-rsa-key.pem
      issuer: eticket.org
      access-token-lifetime: 3600
      refresh-token-lifetime: 345600
